12-09-2023
1. Today we have prepared an mail released Patch for the URL migration 
2. and we have explained the new URL tool approach to the onsite team through mail
3. then we have validated the logs sent by the onsite team about the migration bridge 
4. As per the feedback given by the on-site team,

    Post deployment of beam and enabling the flag, no behavioral change is observed in viewing or exporting the migration reports.

    Also mentioned,
        For the Mobile OTP users, "Migration failure" reports are getting displayed when search is filtered with time but unable to export the records.
        For the password users, "Migration failure" reports are not getting displayed when filtered with time but they can be exported
        When filtered with default time, reports with filter "migration failure" neither displayed nor exported.

   Our observation from the logs are,

    The migration failure reports with the reason "Invalid param verifiers" are not getting displayed with an error "Crash during json encode. Reason : {ucs,{bad_utf8_character_code}}" in the logs.
5. and then i have sent a mail regarding the issue we have faced in url migration for the applicatin 5
 6Application5 consists of 74 urls configured in the v3 instance out of which only 63 URLs are successfully migrated and 11 URLs are failed to migrate with the reason:

"{'EXIT',{badarg,[{erlang,binary_to_integer,[<<"TLA_Auth_Pre">>],[]},{utils,ensure_integer,1,[{file,"src/utils.erl"},{line,261}]},{url_requests,'-validate_url_parameters/1-fun-0-',1,[{file,"src/url_requests.erl"},{line,358}]},{lists,filtermap,2,[{file,"lists.erl"},{line,1315}]},{lists,filtermap,2,[{file,"lists.erl"},{line,1319}]},{url_requests,add_url,3,[{file,"src/url_requests.erl"},{line,9}]},{urls_migrate,insert_into_db,3,[{file,"src/urls_migrate.erl"},{line,115}]},{urls_migrate,getfields,3,[{file,"src/urls_migrate.erl"},{line,56}]}]}}" which is yet to be solved
6. then we have been explained about the utility for the snorkelbx client to update certificate when the tool runs and old and new certificate should be appended inside the tool and tool should be delivered along with the product to avoid error cases 
7. So when ever the bx responds to the tx it responds with the new certificate updated by the utility and the same shouldbe updated in the SnorkelTXinline.